---
# vars file for java
java_packages:
  jre:
    CentOS:
      - java-1.{{ java_version }}.0-{{ java_type }}
    Debian:
      - openjdk-{{ java_version }}-jre
  jdk:
    CentOS:
      - java-1.{{ java_version }}.0-{{ java_type }}-devel
    Debian:
      - openjdk-{{ java_version }}-jre
